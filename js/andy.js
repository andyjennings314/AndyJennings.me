//Misc JS code - I should prune this and remove it
//Toggle the active state for the top menu when Toggle button is tapped
$("a.ToggleMenuButton").click(function () {
    $("body").toggleClass("active");
});
